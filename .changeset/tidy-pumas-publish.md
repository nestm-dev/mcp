---
"@nestm/mcp": patch
---

Publish fixed-group packages sequentially and verify each registry tarball before continuing so
trusted-publisher credential exchanges cannot leave a release only partially available.
