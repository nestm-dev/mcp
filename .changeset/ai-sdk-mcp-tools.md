---
"@nestm/mcp-client": patch
---

Add the optional AI SDK tool adapter for sanitized descriptors and host-protected invocation, preserving exact names, inputs and cancellation without adding AI to core consumers.
