---
"@nestm/mcp-conformance": patch
---

Expose report schemas, immutable types and JSON validation through the browser-safe `/report` subpath. Use standard UTF-8 encoding without Node globals while preserving report size and consistency checks.
