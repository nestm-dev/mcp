# Server-side development proxy target. The browser only talks to same-origin /api.
CONTROL_PLANE_API_URL=http://127.0.0.1:3400

# Must exceed the API's combined operation-drain, shutdown, and replacement-connect budgets.
NEXT_PUBLIC_CONTROL_PLANE_REQUEST_TIMEOUT_MS=45000
